/*
    Admin
 */

function todoApp(state = [], action) {
    return state
}

import { combineReducers, createStore } from 'redux'
const reducer = combineReducers({ mmmmmm});
const store = createStore(reducer);
